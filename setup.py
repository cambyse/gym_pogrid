#!/usr/bin/env python

from setuptools import setup

setup(name='gym_pogrid',
      version='0.0.1',
      install_requires=['gym','pygame','scikit-image','matplotlib']  # And any other dependencies foo needs
)  
